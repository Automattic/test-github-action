name: My Action

on:
  workflow_call:
    inputs:
      username:
        required: true
        type: string

jobs:
  print-input:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo "username = ${{ inputs.username }}"
